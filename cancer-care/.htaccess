# Cancer Care - basic hardening for XAMPP/Apache

# Prevent directory listing
Options -Indexes

# Protect sensitive folders from direct listing (config/includes/models are .php-only,
# but this adds defense in depth for non-php files accidentally placed there)
<FilesMatch "\.(sql|md|log)$">
    Require all denied
</FilesMatch>
